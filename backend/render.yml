services:
  - type: web
    name: speaker-recognition-api
    env: python
    buildCommand: |
      bash build.sh
      pip install -r requirements.txt
    startCommand: PATH="./bin:$PATH" uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
